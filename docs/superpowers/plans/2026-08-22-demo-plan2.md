# 《석사과정》 데모 — 계획 2: 콘텐츠 완성

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 관통 슬라이스에 남은 콘텐츠(비주얼 업그레이드, 오프닝, 사이클 ②, 추격전, 사운드, 파편 플래시)를 채워 스펙 v3의 데모를 완성한다.

**Architecture:** 계획 1의 씬 매니저/페이즈 구조를 그대로 사용 — 미구현 페이즈에 실제 씬을 register하면 유실 카드가 자연히 사라진다. 복도는 오프닝과 추격전이 공유하는 빌더 모듈. 추적자 AI는 Codex가 구현한 `ChaserAI`(작업 #2, 인터페이스 고정)를 소비하고, 사운드는 파일 없이 Web Audio 프로시저럴 합성(Codex 작업 #3, 인터페이스 고정)으로 해결해 빌드 용량과 라이선스 문제를 제거한다.

**Tech Stack:** 계획 1과 동일 (Three.js + Vite + TS + vitest, esbuild 프로덕션 빌드).

**Spec:** `docs/superpowers/specs/2026-08-22-lab-horror-demo-design.md` (v3)

## Global Constraints (계획 1과 동일 + 추가)

- 계획 1의 Global Constraints 전부 유지 (base './', 그림자 금지, 단일 셰이더 패스, rmSync 금지 등).
- 인물은 얼굴 없는 실루엣 유지 — 눈·코·입 지오메트리 금지. 섬뜩함은 자세·시선 추적·정지로만.
- 사운드는 전부 프로시저럴 합성 — 오디오 파일 추가 금지 (용량·라이선스 0).
- Codex 산출물(`ChaserAI`, `SoundSynth`)의 인터페이스는 지시서에 고정된 것을 그대로 소비하고 수정하지 않는다. 도착 전에는 동일 인터페이스의 임시 스텁으로 통합을 진행한다.

## File Structure

```
src/world/person.ts        # 얼굴 없는 인물 빌더 (서기/앉기, 머리 시선 추적)
src/world/props.ts         # 소품 빌더: 의자·책장·프린터·창문·벽시계(??:??)·문·명패
src/world/corridor.ts      # 복도 빌더 (오프닝/추격 공유, 밝음/어둠 변형)
src/scenes/opening.ts      # 오프닝 시퀀스 (405 반복 → 정문 오류 → 출입증 → 시계 파괴)
src/scenes/lab.ts          # (수정) 생활감 소품 + 인물 교체 + 사이클 ①/② 이중 모드
src/ui/minigame2.ts        # 샘플 분류 미니게임 (판정 순수 함수 + DOM)
src/scenes/chase.ts        # 추격전 (ChaserAI 통합, 원잡힘 게임오버, 자동 완화)
src/chase/chaserAI.ts      # ← Codex 작업 #2 산출물 (지시서 docs/codex-tasks/02-chaser-ai.md)
src/audio/soundSynth.ts    # ← Codex 작업 #3 산출물 (지시서 docs/codex-tasks/03-sound-synth.md)
src/ui/flash.ts            # 글리치 전환 파편 플래시 (캔버스 드로잉)
tests/minigame2.test.ts    # 분류 판정 테스트
tests/chaserAI.test.ts     # ← Codex 작업 #2에 포함
```

---

### Task 1: 인물 실루엣 + 연구실 생활감 (사용자 요청 반영)

**Files:** Create `src/world/person.ts`, `src/world/props.ts` / Modify `src/scenes/lab.ts`

**Interfaces:**
- Produces: `makePerson(opts?: { seated?: boolean }): { group: THREE.Group; lookAt(target: THREE.Vector3, dtMs: number): void }` — lookAt은 머리만 **초당 최대 0.15rad**로 아주 천천히 목표를 향해 회전 (섬뜩함의 핵심: 눈치채기 어려운 속도)
- Produces: `props.ts` — `makeChair() / makeShelf() / makePrinter() / makeWindow(w,h) / makeWallClock() / makeDoor() / makePlate(text)` 전부 `THREE.Group | THREE.Mesh` 반환. `makeWallClock`은 CanvasTexture로 "??:??" 표시. `makePlate(text)`는 CanvasTexture 문 명패 (405호 등).

- [ ] **Step 1: person.ts** — 몸통(캡슐 근사: 원기둥+구), 머리(구), 팔(가는 박스 2), 무광 어두운 재질(0x4a4a58). seated는 다리 없이 낮게. 머리 회전은 quaternion slerp 대신 yaw 보간(단순).

```ts
import * as THREE from 'three'

const BODY_M = new THREE.MeshLambertMaterial({ color: 0x4a4a58 })

export function makePerson(opts: { seated?: boolean } = {}) {
  const g = new THREE.Group()
  const h = opts.seated ? 0.95 : 1.45 // 몸통 상단 높이
  const torso = new THREE.Mesh(new THREE.CylinderGeometry(0.16, 0.2, opts.seated ? 0.55 : 0.9, 10), BODY_M)
  torso.position.y = h - (opts.seated ? 0.28 : 0.45)
  const head = new THREE.Mesh(new THREE.SphereGeometry(0.115, 12, 10), BODY_M)
  head.position.y = h + 0.16
  const armL = new THREE.Mesh(new THREE.BoxGeometry(0.06, 0.5, 0.06), BODY_M)
  armL.position.set(-0.24, h - 0.3, 0.05)
  const armR = armL.clone(); armR.position.x = 0.24
  if (!opts.seated) {
    const legs = new THREE.Mesh(new THREE.CylinderGeometry(0.14, 0.16, 0.7, 8), BODY_M)
    legs.position.y = 0.35
    g.add(legs)
  }
  g.add(torso, head, armL, armR)
  let headYaw = 0
  return {
    group: g,
    lookAt(target: THREE.Vector3, dtMs: number) {
      const local = g.worldToLocal(target.clone())
      const want = Math.atan2(local.x, local.z)
      const maxTurn = 0.15 * (dtMs / 1000) // 아주 천천히
      headYaw += Math.max(-maxTurn, Math.min(maxTurn, want - headYaw))
      head.rotation.y = headYaw
    },
  }
}
```

- [ ] **Step 2: props.ts** — 각 소품은 박스 조합. makeWallClock: 256x128 캔버스에 어두운 배경 + 붉은 "??:??" 세그먼트 폰트풍 텍스트 → CanvasTexture → PlaneGeometry(0.5, 0.25). makeWindow: 짙은 남색(0x0d1020) emissive 평면 + 창틀 박스 4개 — "창밖은 항상 밤". makePlate(text): 128x64 캔버스 흰 바탕 검은 텍스트.

- [ ] **Step 3: lab.ts 개조** — 동료 박스 2개를 makePerson(seated)로 교체(각 책상 의자에), update에서 `person.lookAt(camera.position, dt)` 호출. 소품 배치: 의자 4(책상 앞), 책장 2(-z 벽), 프린터 1(+x 벽 구석), 창문 2(-x 벽), 벽시계 1(+z 벽, 눈에 띄는 위치 — 플레이어가 반드시 보게), 문 1(+z 벽, 추후 복도 연결 암시), 바닥 종이 뭉치 몇 개(얇은 박스). 라이팅 재검(밝기 유지).

- [ ] **Step 4: 수동 확인** — dev로 연구실 진입, 인물이 사람으로 읽히는지 / 머리가 천천히 따라오는지 / 벽시계 ??:??가 보이는지. 60fps 유지 확인.

- [ ] **Step 5: Commit** — `feat: 인물 실루엣과 연구실 생활감 (시선 추적, ??:?? 벽시계)`

---

### Task 2: 복도 빌더 + 오프닝 씬

**Files:** Create `src/world/corridor.ts`, `src/scenes/opening.ts` / Modify `src/main.ts` (register 'opening')

**Interfaces:**
- Produces: `buildCorridor(opts: { dark: boolean }): CorridorRig`

```ts
export interface CorridorRig {
  group: THREE.Group
  // 복도는 -z 방향으로 길이 L의 직선 + 양옆 문. 세그먼트 반복 배치.
  length: number
  doorMeshes: THREE.Mesh[]        // 상호작용/충돌 판정용
  setPlate(i: number, text: string): void  // i번째 문 명패 교체
  endDoorZ: number                // 끝 문(정문/면담실)의 z
}
```

- 밝은 변형: 형광등 emissive 줄 + 연구실 톤. 어두운 변형: 조명 제거, 붉은 emissive 비상등 소수 + `scene.fog = new THREE.Fog(0x050505, 2, 14)`.

- [ ] **Step 1: corridor.ts 구현** — 세그먼트(폭 2.4m, 길이 4m) x 8개 직선 배치. 각 세그먼트 양옆에 문+명패. 문은 살짝 어두운 색, 명패는 makePlate 재사용.

- [ ] **Step 2: opening.ts 시퀀스** — 대본 (스펙 §3, 축약판 기준 초과 금지):
  1. 검은 화면 카드 "21:47" (1.2s) → 자막 "저 먼저 가보겠습니다." / 동료(오프스크린) "어. 내일 봐."
  2. 밝은 복도 스폰, 클릭 게이트 → FPS. 자막 "정문으로 나가자."
  3. 명패는 처음엔 401~408. 플레이어가 z<-10 지점을 지나면 **시야 밖에서 전 명패를 405로 교체** + 독백 자막 "…또 405호?" + 글리치 스팅 (조작 시작 30~40초 지점)
  4. z<-24 (7세그먼트째) 통과 시 플레이어를 z=-8로 **문 프레임 통과 프레임에 텔레포트** (비유클리드 규칙: 시야 가림 순간만) — 2회 반복 후
  5. 끝 문 도달 → 카드 리더 오류 연출: 붉은 화면 플래시 + 자막 "출입 권한을 확인할 수 없습니다. / 등록되지 않은 구성원입니다." + 정신력 -10
  6. 암전 → 자막 시퀀스 (교수 목소리): "어디 갔다 왔어?" / "이걸 두고 가면 어떡해." / "연구실 사람은 이거 없으면 안 되지." → 출입증 UI 강제 표시(showBadge) → "내일 일찍 와."
  7. 시계 카드: "21:47"이 치지직거리며 "??:??"로 깨짐 (글리치 펄스) → addJournal('opening') → advance()

- [ ] **Step 3: main.ts에 register('opening', makeOpening)** — 유실 카드 1개 소멸.

- [ ] **Step 4: 수동 확인** — 조작 시작~40초 내 405 반복 체감, 텔레포트가 눈에 안 띄는지, 전체 90초 내 종료.

- [ ] **Step 5: Commit** — `feat: 오프닝 — 뒤틀린 복도, 정문 오류, 출입증, 시계 파괴`

---

### Task 3: 사이클 ② 샘플 분류 미니게임

**Files:** Create `src/ui/minigame2.ts`, `tests/minigame2.test.ts` / Modify `src/scenes/lab.ts` (사이클 모드 분기), `src/main.ts` (register 'cycle2')

**Interfaces:**
- Produces: `resolveSampleGame(errors: number): { trust: number; aptitude: number; journal: 'cycle2_good' | 'cycle2_bad' }` — 스펙 §6-2: 오류 0~1 → {+5, +15, good} / 2~3 → {0, 0, bad} / 4+ → {-15, 0, bad}
  - 주의: 2~3 오류의 일지는 'cycle2_bad'("나갈 이유를 먼저 만들어라") — 의도적 적당함도 탈출 전략이므로.
- Produces: `runSampleMinigame(state, overlay, fx): Promise<void>` — 튜브 6개, 각각 라벨 지시(보관/폐기)와 실제 내용 표시 → 클릭 → 보관/폐기 버튼 2클릭 방식. 치아 튜브 1개(라벨은 "보관") — 확대 버튼 누르면 정신력 -10 + 확대 연출. 종료 전 폐기물통 "톡, 톡" 자막 → 접근 선택지 → "선배님." (정신력 -10, 일지 단서)

- [ ] **Step 1: 판정 테스트 작성** (경계값 1/2/3/4 포함)

```ts
import { describe, it, expect } from 'vitest'
import { resolveSampleGame } from '../src/ui/minigame2'

describe('샘플 분류 판정 (스펙 §6-2)', () => {
  it('오류 0~1 = 정확', () => {
    expect(resolveSampleGame(0)).toEqual({ trust: 5, aptitude: 15, journal: 'cycle2_good' })
    expect(resolveSampleGame(1)).toEqual({ trust: 5, aptitude: 15, journal: 'cycle2_good' })
  })
  it('오류 2~3 = 적당 (스탯 변화 없음, bad 일지)', () => {
    expect(resolveSampleGame(2)).toEqual({ trust: 0, aptitude: 0, journal: 'cycle2_bad' })
    expect(resolveSampleGame(3)).toEqual({ trust: 0, aptitude: 0, journal: 'cycle2_bad' })
  })
  it('오류 4+ = 엉망', () => {
    expect(resolveSampleGame(4)).toEqual({ trust: -15, aptitude: 0, journal: 'cycle2_bad' })
  })
})
```

- [ ] **Step 2: 구현 + 테스트 통과**

- [ ] **Step 3: lab.ts 이중 모드** — `makeLab(cycle: 1 | 2)`: cycle 2는 동선이 냉동고 중심 (자막 "샘플 정리를 시작하자"), 동료 대화 내용 교체 ("그거 원래 그래."), 종료 시 모니터 메시지 카드 "면담실로 오게. — 교수" → advance. main.ts에 `register('cycle2', () => makeLab(2))`.

- [ ] **Step 4: 수동 확인 + Commit** — `feat: 사이클 ② 샘플 분류 — 치아, 폐기물통, 교수 호출`

---

### Task 4: 추격전 씬 (ChaserAI 통합)

**Files:** Create `src/scenes/chase.ts` / Modify `src/main.ts` (register 'chase'), `src/input/controls.ts` (Shift 달리기 노출)

**Interfaces:**
- Consumes: `ChaserAI` (Codex 작업 #2 — `docs/codex-tasks/02-chaser-ai.md`의 고정 인터페이스). 미도착 시 동일 인터페이스의 스텁(순찰만)으로 통합을 먼저 완성.
- Consumes: `SoundSynth` (Task 5) — 심장소리 강도.
- controls에 `isRunning: boolean` getter 추가 (Shift 누름 + 이동 중).

- [ ] **Step 1: 어두운 복도 셋업** — buildCorridor({dark:true}), 붉은 비상등, 안개. 플레이어 스폰 z=0, 면담실 문 z=endDoorZ. 자막 "복도가 어둡다. 면담실로 가야 한다."

- [ ] **Step 2: 추적자 표현** — 전신 노출 금지: 어두운 세장한 실루엣(makePerson 변형, 키 2.2m) + 위치는 ChaserAI output. **비상등 사이에서만 실루엣이 보이게** 어두운 재질. 근접(거리<4)시 `fx.set({glitch: (4-d)/4 * 0.5})` + 심장 강도.

- [ ] **Step 3: AI 연결** — 매 update: occlusion은 추적자→플레이어 레이캐스트(복도 벽 대상), `chaser.update({dtMs, playerPos, playerIsRunning: controls.isRunning, occluded})`. 달리기(Shift)는 이동 속도 3.6m/s.
- 완화 규칙 (스펙 §8): `fails = state.chaseFails`; hearRadius·sightRadius에 `Math.max(0.4, 0.8 ** Math.max(0, fails - 1))` 곱, speedChase에 `Math.max(0.7, 0.9 ** Math.max(0, fails - 3))` 곱. 화면 표시 금지.

- [ ] **Step 4: 잡힘/도착 처리** — caught: fx 급속 글리치 1.0 → 카드 "기록 손상 — 해당 구간을 재복원합니다" (1.8s) → state.chaseFails++ → addJournal('chase_failed') → 씬 리셋(chaser.reset(), 플레이어 스폰 복귀). 면담실 문 z 도달: addJournal('chase_done') → advance() (엔딩 시네마틱).

- [ ] **Step 5: 수동 확인** — 잡히고 재시작 3초 내, 3회 실패 후 체감 완화, 도착 시 엔딩 연결. Commit — `feat: 추격전 — 원잡힘 게임오버, 자동 완화, 면담실 도착`

---

### Task 5: 프로시저럴 사운드 (Codex 작업 #3)

**Files:** Create `docs/codex-tasks/03-sound-synth.md` (지시서) / Codex 산출: `src/audio/soundSynth.ts` / Modify 씬들 (사운드 훅)

**Interfaces (지시서에 고정할 계약):**

```ts
export type OneShot = 'footstep' | 'glitch' | 'beep_error' | 'beep_ok' | 'knock' | 'whisper'
export type Loop = 'hum' | 'heartbeat'
export class SoundSynth {
  constructor(ctx: AudioContext)
  start(name: Loop, volume?: number): void
  stop(name: Loop): void
  play(name: OneShot, volume?: number): void
  setHeartRate(v: number): void   // 0~1 → 50~140 BPM
  dispose(): void
}
```

- 전부 OscillatorNode/BufferSource(화이트노이즈)/BiquadFilter 합성. 오디오 파일 금지.
- hum = 60Hz 험 + 필터 노이즈 미세 / footstep = 저역 노이즈 버스트 80ms / heartbeat = 저역 사인 더블탭 / glitch = 비트크러시풍 노이즈 스퍼트 / beep_error = 하강 사각파 2연타 / beep_ok = 단일 상승음 / knock = 중저역 임펄스 3연타 (톡, 톡, 톡) / whisper = 대역 협소 노이즈 1.2s.

- [ ] **Step 1: 지시서 작성·커밋** (위 계약 + "확인 질문 없이 구현" 헤더)
- [ ] **Step 2: (Codex 완료 후) 검증** — 각 사운드 콘솔 트리거 재생 확인, 기존 테스트 유지
- [ ] **Step 3: 씬 연결** — title 게이트 후 hum 시작(연구실), 이동 중 footstep, 오프닝 카드 오류 beep_error, 추격 심장 setHeartRate(근접도), 전환 glitch, 폐기물통 knock→whisper
- [ ] **Step 4: Commit** — `feat: 프로시저럴 사운드 연결 (Codex 작업 #3)`

---

### Task 6: 파편 플래시 + 마무리 폴리시

**Files:** Create `src/ui/flash.ts` / Modify `src/scenes/sceneManager.ts` (전환 중 플래시)

- [ ] **Step 1: flash.ts** — `flashFragment(root: HTMLElement, kind: 'grill' | 'exam' | 'dish' | 'redlight')`: 전체 화면 캔버스에 2프레임(약 90ms)짜리 단순 도형 구성 (grill=어두운 배경+붉은 원형 격자, exam=흰 종이+검은 줄+"다음 중" 텍스트, dish=어두운 원+내부 얼룩, redlight=붉은 세로 광선) 후 제거.
- [ ] **Step 2: sceneManager 전환에 랜덤 아닌 **페이즈별 고정** 파편 삽입 (결정적: cycle1→2 = exam, 2→chase = redlight, chase→ending = dish, opening→cycle1 = grill).
- [ ] **Step 3: 최종 관통 플레이 (전 페이즈 실제 씬) + 60fps 확인 + Commit — `feat: 전환 파편 플래시`**

---

### Task 7: 통합 검증 + 배포

- [ ] **Step 1:** `npm test && npm run build` 전체 그린, 1회차 완주 플레이타임 측정 (10~13분 목표, 초과 시 오프닝/카드 시간 압축)
- [ ] **Step 2:** main 푸시 → Pages 배포 → 배포 URL에서 최소 1회 완주
- [ ] **Step 3:** codex-log.md에 작업 #2·#3 기록 보강, 메모리 갱신

## Self-Review 결과

- **스펙 커버리지**: v3의 §3 플로우 전 페이즈가 실제 씬으로 채워짐. 남는 유보: 숨기 메커니즘(1차 컷 확정), 한글 폰트 서브셋(시스템 폰트로 충분하면 생략 가능 — Task 7에서 판단), 데모 영상·활용 설명(제출물 — 별도 후속).
- **플레이스홀더**: 없음. Codex 산출물 2건은 지시서에 인터페이스가 고정돼 있고 스텁 대체 경로 명시.
- **타입 일관성**: CorridorRig·makePerson 반환형·resolveSampleGame·ChaserAI/SoundSynth 계약 교차 확인 완료.
